# 🕵️‍♂️ Android Fraud Detection — Тестовое задание

> **Цель**: Разработать модель машинного обучения для классификации Android-устройств на **мошеннические (F)** и **легитимные (G)** на основе технических параметров и списка установленных приложений.

![Python](https://img.shields.io/badge/Python-3.9%2B-blue?logo=python)
![LightGBM](https://img.shields.io/badge/LightGBM-✅-green)
![XGBoost](https://img.shields.io/badge/XGBoost-✅-orange)
![Scikit-learn](https://img.shields.io/badge/scikit--learn-✅-yellow)
![Pandas](https://img.shields.io/badge/Pandas-✅-purple)

---

## 📌 Оглавление

- [Описание задачи](#описание-задачи)
- [Датасеты](#датасеты)
- [Подход к решению](#подход-к-решению)
- [Извлечённые признаки](#извлечённые-признаки)
- [Модели и стекинг](#модели-и-стекинг)
- [Оценка качества](#оценка-качества)
- [Структура проекта](#структура-проекта)
- [Как запустить](#как-запустить)
- [Результаты](#результаты)
- [Возможные улучшения](#возможные-улучшения)

---

## 🎯 Описание задачи

Устройства клиентов могут быть использованы мошенниками для:

1. **Социальной инженерии** — вход в чужие аккаунты с подозрительных устройств.
2. **Бонусного мошенничества** — массовая регистрация аккаунтов через эмуляторы или фермы устройств.

Задача — **обнаружить такие устройства** по их техническим характеристикам и списку установленных приложений.

---

## 📂 Датасеты

| Файл | Описание |
|------|----------|
| `android_device_info.csv` | Основная информация об устройстве (`agent_id`, `target`, Build-параметры, флаги безопасности) |
| `android_packages.csv.gz` | Список установленных приложений в формате JSON (`cert`, `installed`, `sha1`) |

🔗 Оба файла связаны по полю `agent_id`.

> 💡 Подробнее о полях `Build.*` — [Android OS Build Reference](https://developer.android.com/reference/android/os/Build)

---

## 🧠 Подход к решению

1. **EDA и профилирование данных** → `utils/analyze_data.py`
2. **Feature engineering**:
   - Обработка Build-информации
   - Анализ пакетов: легитимные/подозрительные приложения
   - Расчёт энтропии серийного номера
   - Выявление общих/редких отпечатков устройств
3. **Создание финального датасета** → `utils/build_final_dataset.py`
4. **Обучение ансамбля моделей**:
   - LightGBM + XGBoost
   - Честный стекинг с hold-out и OOF-валидацией
5. **Оценка бизнес-метрик** (FP/FN стоимость)

---

## 🔍 Извлечённые признаки

### 📦 Из пакетов
- `n_packages`, `n_unique_certs`
- `n_legit_apps` — количество официальных приложений (WhatsApp, Telegram, Сбер и т.д.)
- `has_suspicious_pkg` — наличие эмуляторов (Nox, BlueStacks, LDPlayer и др.)
- `install_span_hours` — временной разброс установки приложений
- `frac_legit_certs` — доля сертификатов, характерных для легитимных устройств

### 🛠️ Из Build-информации
- `serial_entropy` — энтропия серийного номера (низкая = подозрительно)
- `is_common_serial` — совпадение с известным "массовым" серийником
- `is_test_keys` — сборка с тестовыми ключами (часто в эмуляторах)
- `is_suspicious_host` — хосты типа `mi-server`, `ota-bd`, `cn-`, `kvm` и др.
- `is_old_sdk` — Android SDK < 28
- `is_common_fprint` / `is_rare_fprint` — частота отпечатка устройства

### ⚠️ Комбинированные флаги
- `risky_combo_1`: включены недоверенные источники + отсутствует PIN + включен режим разработчика
- `emulator_pattern`: старый SDK + отладчик + общий серийник
- `low_legit_ratio`: менее 10% легитимных приложений

---

## 🤖 Модели и стекинг

- **Базовые модели**:
  - `LightGBM` (с early stopping, `class_weight='balanced'`)
  - `XGBoost` (с `scale_pos_weight` по соотношению классов)
- **Мета-модель**: `LogisticRegression` на OOF-предсказаниях
- **Валидация**: стратифицированный KFold + hold-out split
- **Реализация**: `model/stacking.py`

> ✅ Используется **честный стекинг** — мета-модель обучается только на OOF, а финальная оценка — на отдельном hold-out.

---

## 📊 Оценка качества

Метрики:
- **ROC-AUC**, **PR-AUC**
- **F1-macro**, **G-mean**, **MCC**
- **Brier score**

Бизнес-метрики:
- Стоимость FP (блокировка легитимного клиента): **1200 ₽**
- Стоимость FN (пропуск мошенника): **5000 ₽**
- Рассчитывается **экономия** по сравнению с baseline («пускать всех»)

> 📌 Порог бинаризации по умолчанию: **0.4** (можно оптимизировать через `src/thresholding.py`)

---

## 🗂️ Структура проекта
```
├── main.py # Главный скрипт запуска (raw/preprocessed/auto)
├── validate.py # Оценка уже обученной модели
├── src/
│ ├── features.py # Полный препроцессинг
│ ├── model_lgb.py # LightGBM
│ ├── model_xgb.py # XGBoost
│ ├── models.py # Устаревшая версия (не используется)
│ ├── metrics.py # Оценка + бизнес-метрики
│ └── thresholding.py # Поиск оптимального порога
├── model/
│ ├── stacking.py # ✅ Основной стекинг (с hold-out)
│ └── oof_stacking.py # Альтернативная версия (не используется)
├── utils/
│ ├── build_final_dataset.py # Создание train_dataset_6.csv
│ ├── analyze_data.py # EDA → JSON-профиль
│ ├── balance.py # Балансировка 50/50 (для экспериментов)
│ ├── sample_dataset.py # Разделение на train/test
│ └── preobrazovanie.py # Распаковка .gz
└── models/ # Сохранённые модели (joblib)
```

## ▶️ Как запустить

### Требования
```bash
pip install -r requirements.txt
```

## Варианты запуска
1. Если у вас уже есть train_dataset_6.csv и test_dataset_6.csv:
```
python main.py --mode preprocessed
```
> Если у вас нет таких файлов можете их создать в build_final_dataset.py

2. Если у вас сырые данные (android_device_info.csv, android_packages.csv.gz):
   ```
   python main.py --mode raw
   ```
3. Автоматический выбор:
   ```
   python main.py --mode auto  # (по умолчанию)
   ```
4. Только оценка модели (без обучения):
```
python validate.py --model-dir models
```

## 🏆 Результаты

## 🏆 Результаты на фиксированном тестовом наборе

Модель оценена на **фиксированном тесте** (`test_dataset_6.csv`, 5 792 устройства, доля Fraud = 17.9%).  
Все метрики рассчитаны при пороге бинаризации **0.4**.

### 📊 Ключевые метрики качества

| Метрика                | Значение |
|------------------------|----------|
| **ROC-AUC**            | 0.9374   |
| **PR-AUC**             | 0.9047   |
| **Accuracy**           | 0.9301   |
| **Balanced Accuracy**  | 0.8975   |
| **F1-macro**           | 0.8848   |
| **G-mean**             | 0.8960   |
| **MCC**                | 0.7706   |
| **Brier Score**        | 0.0682   |

### 🧮 Confusion Matrix
[[4509 246] ← Genuine (всего: 4755)
[ 159 878]] ← Fraud (всего: 1037)

- ✅ **TN (верно легитимные)**: 4 509  
- ⚠️ **FP (ложные блокировки)**: 246  
- ❌ **FN (пропущенные мошенники)**: 159  
- ✅ **TP (верно выявленные мошенники)**: 878  

### 📋 Classification Report

|             |precision |  recall  |f1-score  |support|
|-------------|----------|----------|------------------|
|Genuine      |0.97      |0.95      |0.96      |4755   |
|Fraud        |0.78      |0.85      |0.81      |1037   |
|accuracy     |          |          |0.93      |5792   |
|macro avg    |0.87      |0.90      |0.88      |5792   |
|weighted avg | 0.93     |0.93      |0.93      |5792   |

---

### 💼 Бизнес-влияние (гипотезы для России, 2025)

> 💡 **Стоимость решений**:
> - **FP** (блокировка легитимного пользователя): **1 200 ₽**
> - **FN** (пропуск мошенника): **5 000 ₽**

#### 💰 Расчёт убытков и экономии

```text
Общий бизнес-убыток модели:
= 246 × 1 200 ₽ + 159 × 5 000 ₽
= 295 200 ₽ + 795 000 ₽
= **1 090 200 ₽**
```

Если бы модель не использовалась, все мошенники прошли бы незамеченными:
→ Убыток = 1 037 × 5 000 ₽ = 5 185 000 ₽

✅ Экономия благодаря модели:
5 185 000 ₽ – 1 090 200 ₽ = 4 094 800 ₽

📈 Средняя экономия на одно устройство: 706.98 ₽

🎯 Вывод: модель приносит значительную бизнес-ценность и эффективно балансирует между безопасностью и пользовательским опытом.
